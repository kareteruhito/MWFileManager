<Window x:Class="MWFileManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MWFileManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel LastChildFill="True">
        <!-- メニュー -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="ファイル">
                <MenuItem Header="終了" Click="ExitMenuItem_Click" />
            </MenuItem>
        </Menu>        
        <!-- ステータスバー-->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
        <!-- メインエリア -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel
                    Grid.Column="0"
                    Orientation="Horizontal">
                    <Button
                        Content="⇧"
                        FontSize="16"
                        Width="32"
                        ToolTip="親ディレクトリへ移動"
                        Click="ParentDirButton_Click"
                        Margin="4"
                        Padding="2"/>
                    </StackPanel>
                <TextBox
                    Grid.Column="1"
                    x:Name="AddressBar"
                    VerticalAlignment="Center"
                    Height="32"
                    FontSize="16"
                    Margin="4"/>
                <StackPanel
                    Grid.Column="2"
                    Orientation="Horizontal">
                    <Button
                        Content="↵"
                        FontSize="16"
                        Width="32"
                        ToolTip="アドレスバーのディレクトリへ移動"
                        Click="MoveDirButton_Click"
                        Margin="4"
                        Padding="2" />
                </StackPanel>            
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <TreeView x:Name="DriveTreeView"
                    VirtualizingStackPanel.IsVirtualizing="True"
                    VirtualizingStackPanel.VirtualizationMode="Recycling"
                    SelectedItemChanged="TreeView_SelectedItemChanged"
                    TreeViewItem.Expanded="TreeViewItem_Expanded">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{Binding Icon}"
                                    Width="16"
                                    Height="16"
                                    Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding Name}"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>

                <!-- グリッドセパレータ -->
                <GridSplitter
                    Grid.Column="1"
                    Width="5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Stretch"
                    Background="Gray"
                    ShowsPreview="True"
                    ResizeDirection="Columns"
                    Margin="2"/>
                <ListView
                    x:Name="FileListView"
                    ItemsSource="{Binding Files}"
                    SelectionMode="Extended"
                    Grid.Column="2"
                    Margin="8">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Padding" Value="4"/>
                            <EventSetter Event="MouseDoubleClick"
                                        Handler="ListViewItem_MouseDoubleClick" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="コピー"
                                Click="Copy_Click" />
                            <MenuItem Header="移動"
                                Click="Move_Click" />
                            <MenuItem Header="削除"
                                Click="Delete_Click" />
                            <MenuItem Header="貼り付け"
                                x:Name="Context_MenuItem_Paste"
                                Visibility="Collapsed"
                                Click="Paste_Click" />
                        </ContextMenu>
                    </ListView.ContextMenu>
                    <ListView.View>
                        <GridView AllowsColumnReorder="False">
                            <GridViewColumn
                                Header="アイコン"
                                Width="48">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image
                                            Source="{Binding Icon}"
                                            Width="24"
                                            Height="24"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn>
                                <GridViewColumnHeader
                                    Width="300"
                                    Content="名前"
                                    Click="Name_Header_Click" />
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            HorizontalAlignment="Stretch"
                                            TextAlignment="Left"
                                            Text="{Binding DisplayName}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
